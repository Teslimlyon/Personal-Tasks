applyTwice :: (a -> a) -> a -> a
applyTwice f x = f (f x)

half :: Int -> Int
half x = x `div` 2

main :: IO ()
main = do
  print $ applyTwice half 100
  --Output 25




To compare that remainder to something (like 0), you must use a comparison operator:

== → checks if they are equal

/= → checks if they are not equal

